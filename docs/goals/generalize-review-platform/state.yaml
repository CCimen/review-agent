# Goal Maker v2 state.yaml
# Board truth lives here. goal.md is the editable charter; notes/ holds long receipts only.

version: 2

goal:
  title: "Generalize the pull-request review platform"
  slug: "generalize-review-platform"
  kind: open_ended
  tranche: "Map Eneo coupling, implement the first clean-break generic identity/configuration slice without review behavior change, verify it, and audit it."
  status: active

rules:
  pm_owns_state: true
  one_active_task: true
  max_write_workers: 1
  no_implementation_without_worker_or_pm_task: true
  no_completion_without_judge_or_pm_audit: true
  planning_is_not_completion: true
  queued_required_worker_blocks_completion: true

agents:
  scout: installed
  worker: installed
  judge: installed

continuity:
  rotate_threads: false
  rotation_authorized_by: null
  rotate_after_completed_write_tasks: 2
  rotate_after_hours: 6
  rotate_when_slow: true
  current_thread_id: null
  previous_thread_id: null
  rotation_count: 0
  completed_write_tasks_in_thread: 8
  handoff_note: notes/handoff.md
  last_handoff_at: null
  last_verified_revision: "b797ebf"

active_task: T999

tasks:
  - id: T000
    type: pm
    assignee: PM
    status: done
    reasoning_hint: default
    objective: "Import and publish the exact validated Eneo reviewer baseline."
    inputs:
      - "eneo-ai-bot/eneo-security-review-infra at 4c8cb1b0590575852ead6767890a677c316089b9"
    constraints:
      - "No runtime, policy, schema, or naming changes."
      - "Preserve upstream history."
    expected_output:
      - "Baseline on CCimen/review-agent main"
      - "Green validation receipt"
    receipt:
      result: done
      summary: "Imported the exact upstream tree, connected its history, and pushed the validated baseline to main."
      changed_files:
        - "Validated upstream repository snapshot"
      commands:
        - cmd: "git diff --cached --quiet eneo-upstream/main"
          status: pass
        - cmd: "./scripts/check_bundle.sh"
          status: pass
        - cmd: "git push origin main"
          status: pass
  - id: T001
    type: scout
    assignee: Scout
    status: done
    reasoning_hint: high
    objective: "Map Eneo-specific coupling and identify the smallest behavior-preserving generic identity/configuration slice."
    inputs:
      - README.md
      - .env.example
      - compose.yaml
      - bootstrap/config.yaml
      - bootstrap/SOUL.md
      - bootstrap/workspace/AGENTS.md
      - bootstrap/skills/eneo-pr-review/SKILL.md
      - bootstrap/plugins/eneo_review_tools
      - tools
      - tests
      - docs
    constraints:
      - "Read-only."
      - "Classify visible identity, compatibility identifiers, policy/profile, persistence, deployment, workflow, and test contracts separately."
      - "Prefer exact paths and symbols over directory-layout advice."
      - "Do not assume the proposed clean-architecture directory tree is justified."
    expected_output:
      - "Canonical owner map"
      - "Coupling inventory"
      - "Smallest safe first implementation slice"
      - "Explicit deferrals and stop conditions"
    receipt:
      result: done
      note: "notes/T001-eneo-coupling-map.md"
      summary: "Mapped 926 case-insensitive Eneo references across 75 files; the domain/schema are already generic, while names cluster around compatibility, deployment, workflow, profile policy, and tests."
      evidence:
        - "bootstrap/plugins/eneo_review_tools/tools.py::_allowlisted_repository"
        - "bootstrap/plugins/eneo_review_tools/feedback_bridge.py::load_config"
        - "bootstrap/plugins/eneo_review_tools/review_identity.py"
        - "bootstrap/workspace/AGENTS.md"
        - "compose.yaml"
      spawned_tasks:
        - T002
  - id: T002
    type: judge
    assignee: Judge
    status: done
    reasoning_hint: high
    objective: "Challenge the Scout map and select one reviewable genericization task."
    inputs:
      - "T001 receipt"
      - "Current clean main revision"
      - "Existing replay and bundle validation"
    constraints:
      - "Do not implement."
      - "Prefer deepening existing owners over creating domain/port scaffolding."
      - "Reject PostgreSQL, queue, GitHub App, scanner, Slack, and broad package renames from this tranche."
      - "Require only focused tests that protect observable behavior or compatibility."
    expected_output:
      - "Decision"
      - "Exact Worker objective"
      - "allowed_files"
      - "verify"
      - "stop_if"
      - "Deferred tasks"
    receipt:
      result: done
      decision: "The original compatibility-alias decision was superseded by direct owner instruction: this new repository must use generic names only."
      user_operator_value: "New deployments have one unambiguous generic allowlist setting, with no inherited Eneo configuration path."
      canonical_owner: "Existing consumer-specific allowlist parsing in tools.py and feedback_bridge.py; environment selection no longer needs a separate abstraction."
      constraints:
        - "Missing or blank generic configuration fails closed."
        - "Do not retain ENEO_ALLOWED_REPOSITORIES as an alias."
        - "Do not change repository parsing grammar, review output, schema, routes, tools, or markers."
        - "Do not add configuration permutation or internal wiring tests."
      next_allowed_task: T003
      blocked_tasks:
        - T999
  - id: T003
    type: pm
    assignee: PM
    status: done
    reasoning_hint: default
    objective: "Replace ENEO_ALLOWED_REPOSITORIES with REVIEW_AGENT_ALLOWED_REPOSITORIES across live review, feedback, deployment, operator documentation, and affected tests, with no compatibility alias."
    constraints:
      - "The PM implements and verifies production changes directly."
      - "Subagents may provide read-only research or review only."
      - "Use selective behavior and contract tests; do not add a permutation matrix or internal wiring assertions."
      - "Delete compatibility-only code instead of abstracting it."
    allowed_files:
      - bootstrap/plugins/eneo_review_tools/tools.py
      - bootstrap/plugins/eneo_review_tools/feedback_bridge.py
      - bootstrap/plugins/eneo_review_tools/plugin.yaml
      - compose.yaml
      - .env.example
      - docs/OPERATIONS.md
      - tools/eneo_review_feedback_bridge.py
      - tests/test_failure_status.py
      - tests/test_pr_diff_fallback.py
      - tests/test_run_tools.py
      - tests/test_tools_validation.py
      - tests/test_feedback_bridge.py
      - tests/test_feedback_bridge_entrypoint.py
    verify:
      - "PYTHONPATH=bootstrap/plugins python3 -m unittest -v tests.test_tools_validation.ToolValidationTests.test_allowlist_is_accepted_before_other_input_validation tests.test_tools_validation.ToolValidationTests.test_empty_allowlist_denies_by_default tests.test_feedback_bridge.FeedbackBridgeTests.test_config_uses_generic_repository_allowlist tests.test_feedback_bridge_entrypoint.FeedbackBridgeEntrypointTests.test_env_presence_summary_redacts_secret_values"
      - "PYTHONPATH=bootstrap/plugins python3 -m unittest -v tests.test_tools_validation tests.test_feedback_bridge tests.test_feedback_bridge_entrypoint"
      - "pyright -p ."
      - "Validate Compose with the generic allowlist and verify missing configuration is rejected, using harmless placeholders for other required variables."
      - "rg -n 'ENEO_ALLOWED_REPOSITORIES' --glob '!docs/goals/**' --glob '!.codex/**' . returns no matches."
      - "git diff --check"
      - "./scripts/check_bundle.sh"
    stop_if:
      - "Need files outside allowed_files."
      - "A compatibility or visible-output behavior is ambiguous."
      - "A focused verification fails twice."
      - "The change requires schema, PostgreSQL, GitHub App, scanner, Slack, or deployment-topology work."
      - "Generic-only Compose configuration cannot start or missing configuration does not fail closed."
      - "Repository parsing grammar or membership semantics must change."
    receipt:
      result: done
      summary: "Replaced the Eneo repository allowlist setting with one generic, fail-closed setting and removed compatibility-only code and branded repository fixtures from all touched tests."
      revision: "a5ff3b4"
      changed_files:
        - "Reviewer and feedback configuration owners"
        - "Compose, plugin manifest, environment example, and operations documentation"
        - "Six affected test modules and the feedback entrypoint"
      commands:
        - cmd: "PYTHONPATH=bootstrap/plugins python3 -m unittest -v tests.test_tools_validation tests.test_feedback_bridge tests.test_feedback_bridge_entrypoint tests.test_failure_status tests.test_pr_diff_fallback tests.test_run_tools"
          status: pass
          result: "105 tests"
        - cmd: "pyright -p ."
          status: pass
        - cmd: "docker compose --env-file /dev/null config -q with generic setting"
          status: pass
        - cmd: "docker compose --env-file /dev/null config -q without generic setting"
          status: pass
          result: "rejected as required"
        - cmd: "./scripts/check_bundle.sh"
          status: pass
          result: "457 tests, two replay fixtures, strict Pyright, and YAML"
        - cmd: "codex-peer-loop commit gate"
          status: pass
          result: "green, 9/10, no blockers"
        - cmd: "git push origin main"
          status: pass
  - id: T004
    type: pm
    assignee: PM
    status: done
    reasoning_hint: default
    objective: "Mechanically replace the Eneo runtime namespace: rename the Python plugin package, operator tool filenames/imports, Hermes plugin/toolset/tool contracts, and review skill to review-agent names without behavior changes or aliases."
    constraints:
      - "The PM implements and verifies all production changes directly."
      - "Subagents may provide read-only research or review only."
      - "Use review_agent_tools, review-agent-tools, review_agent toolset, review_agent_* tool names, and review-agent-pr skill names consistently."
      - "Move existing owners; do not duplicate packages, tools, schemas, or registrations."
      - "Do not change policy behavior, environment names other than the completed allowlist, webhook routes, comment markers, persistence schema, or review output in this task."
      - "Update existing behavior/contract tests mechanically; add no new test matrix."
    allowed_files:
      - bootstrap/plugins/eneo_review_tools
      - bootstrap/plugins/review_agent_tools
      - bootstrap/skills/eneo-pr-review
      - bootstrap/skills/review-agent-pr
      - tools/eneo_review_*.py
      - tools/review_agent_*.py
      - bootstrap/config.yaml
      - bootstrap/install.py
      - bootstrap/install.sh
      - Dockerfile
      - compose.yaml
      - pyrightconfig.json
      - scripts/check_bundle.sh
      - tests
      - README.md
      - docs/OPERATIONS.md
      - docs/SECURITY.md
    verify:
      - "rg confirms no eneo_review_tools, eneo-review-tools, eneo_review toolset/tool names, eneo-pr-review skill path, or tools/eneo_review_*.py references remain outside archival goal receipts."
      - "pyright -p ."
      - "PYTHONPATH=bootstrap/plugins python3 -m unittest discover -s tests -v"
      - "./scripts/check_bundle.sh"
      - "python3 tools/review_agent_memory.py --help"
      - "git diff --check"
    stop_if:
      - "A rename requires behavior, schema, policy, webhook-route, marker, or deployment-topology changes."
      - "Dynamic plugin loading would require retaining the old package or command as an alias."
      - "A focused verification fails twice for a non-mechanical reason."
    receipt:
      result: done
      summary: "Moved the Python plugin, Hermes contracts, review skill, operator modules, installed commands, bootstrap paths, imports, documentation references, and existing tests to one review-agent namespace without aliases or duplicate owners."
      revision: "b09bcc3"
      changed_files:
        - "bootstrap/plugins/review_agent_tools"
        - "bootstrap/skills/review-agent-pr"
        - "tools/review_agent_*.py"
        - "Hermes config, installer, Docker, Pyright, bundle, documentation, and affected tests"
      commands:
        - cmd: "PYTHONPATH=bootstrap/plugins python3 -m unittest -v focused plugin/install/docs/module/entrypoint contracts"
          status: pass
        - cmd: "pyright -p ."
          status: pass
        - cmd: "./scripts/check_bundle.sh"
          status: pass
          result: "457 tests, two replay fixtures, strict Pyright, and YAML before deleting one redundant docs-only test"
        - cmd: "PYTHONPATH=bootstrap/plugins python3 -m unittest -v tests.test_docs_contract tests.test_tools_validation.ToolValidationTests.test_plugin_registers_all_declared_tools"
          status: pass
          result: "23 focused tests after the deletion"
        - cmd: "docker compose --env-file /dev/null config -q"
          status: pass
        - cmd: "python3 tools/review_agent_memory.py --help and review_agent_feedback_bridge.py --help"
          status: pass
        - cmd: "runtime namespace rg scan"
          status: pass
          result: "no old package, toolset, tool, skill, command, module, or bootstrap-path names outside archival goal receipts"
        - cmd: "codex-peer-loop commit gate"
          status: pass
          result: "green, 8/10, no blockers; obsolete duplicate test deleted"
        - cmd: "git push origin main"
          status: pass
  - id: T005
    type: pm
    assignee: PM
    status: done
    reasoning_hint: default
    objective: "Replace every remaining ENEO_* runtime and deployment environment name with the corresponding REVIEW_AGENT_* name across code, Compose, examples, operator documentation, and existing tests, with no aliases."
    constraints:
      - "Resume only when the repository owner continues work after the requested overnight checkpoint."
      - "The PM implements and verifies all production changes directly."
      - "Subagents may provide read-only research or review only."
      - "Rename configuration directly; do not add fallback reads, precedence rules, warnings, or compatibility modules."
      - "Keep GitHub credential boundaries separate and preserve fail-closed startup behavior."
      - "Do not change webhook routes, comment markers, policy behavior, persistence schema, or deployment topology in this task."
      - "Update existing behavior/contract tests proportionally; add no configuration matrix."
    expected_output:
      - "Generic REVIEW_AGENT_* configuration in reviewer, publisher, feedback, memory, Compose, examples, and docs"
      - "No ENEO_* references outside archival goal receipts"
      - "Green focused and bundle validation"
    verify:
      - "rg confirms no ENEO_* reference remains outside archival goal receipts."
      - "pyright -p ."
      - "./scripts/check_bundle.sh"
      - "Docker Compose accepts a complete generic configuration and rejects missing required generic secrets."
      - "git diff --check"
    stop_if:
      - "A configuration rename requires aliases or retained old deployment state."
      - "Credential scopes or trust boundaries would change."
      - "A focused verification fails twice for a non-mechanical reason."
    receipt:
      result: done
      summary: "Replaced the remaining Eneo-prefixed environment interface with one generic review-agent contract, preserved separate credentials and fail-closed startup, and removed the last duplicated actor-allowlist key literal."
      revision: "66ee154"
      changed_files:
        - "Reviewer, publisher, feedback, and memory configuration owners"
        - "Compose, plugin manifest, environment example, and operations documentation"
        - "Affected behavior and documentation contract tests"
      commands:
        - cmd: "PYTHONPATH=bootstrap/plugins python3 -m unittest -v affected environment-contract modules"
          status: pass
          result: "186 tests"
        - cmd: "pyright -p ."
          status: pass
          result: "0 errors and 0 warnings"
        - cmd: "./scripts/check_bundle.sh"
          status: pass
          result: "456 tests, two replay fixtures, strict Pyright, and YAML"
        - cmd: "docker compose config --quiet with a complete generic configuration"
          status: pass
        - cmd: "docker compose config --quiet with each required publisher/feedback value empty"
          status: pass
          result: "publisher token, feedback secret, feedback token, and actor allowlist rejected independently"
        - cmd: "tracked-file ENEO_* scan outside archival goal receipts"
          status: pass
          result: "no legacy environment identifiers"
        - cmd: "codex-peer-loop commit gate"
          status: pass
          result: "green, 8/10, no blockers"
  - id: T006
    type: pm
    assignee: PM
    status: done
    reasoning_hint: default
    objective: "Replace Eneo-branded webhook paths and their deployment/workflow/operator references with generic review-agent paths in one explicit public-contract slice."
    constraints:
      - "Resume implementation when the repository owner continues work."
      - "The PM implements and verifies all production changes directly."
      - "Subagents may provide read-only research or review only."
      - "Use one review path and one feedback path; do not retain route aliases or redirects."
      - "Change only route identity and exact affected contracts; do not change authentication, payloads, workflow trigger semantics, comment markers, policy behavior, persistence schema, or topology."
      - "Update existing behavior and workflow tests proportionally; add no route matrix."
    expected_output:
      - "Generic webhook paths in runtime, Compose, workflow examples, scripts, docs, and affected tests"
      - "No Eneo-branded route reference outside archival goal receipts"
      - "Green focused and bundle validation"
    verify:
      - "Focused feedback bridge, workflow, documentation, and smoke/status script tests."
      - "rg confirms no Eneo-branded webhook path remains outside archival goal receipts."
      - "pyright -p ."
      - "./scripts/check_bundle.sh"
      - "Docker Compose renders the generic routes."
      - "git diff --check"
    stop_if:
      - "A route change requires retaining an alias, migrating a live deployment, or changing authentication/payload behavior."
      - "A focused verification fails twice for a non-mechanical reason."
    receipt:
      result: done
      summary: "Replaced the Eneo review and feedback webhook paths, trigger identity, deployment examples, and operator clients with one generic contract; made the status helper configurable, truthful about workflow outcomes, and bounded."
      revision: "411921e"
      changed_files:
        - "Hermes review route and feedback bridge default path"
        - "GitHub trigger workflow, Compose fallback labels, operations documentation, and README ownership"
        - "Smoke and status operator scripts plus focused route/status tests"
      commands:
        - cmd: "PYTHONPATH=bootstrap/plugins python3 -m unittest -v affected route/workflow/status/docs modules"
          status: pass
          result: "48 tests"
        - cmd: "pyright -p ."
          status: pass
          result: "0 errors and 0 warnings"
        - cmd: "./scripts/check_bundle.sh"
          status: pass
          result: "460 tests, two replay fixtures, strict Pyright, and YAML"
        - cmd: "docker compose config --quiet with a complete generic configuration"
          status: pass
        - cmd: "retired webhook path and trigger identity scan outside archival goal receipts"
          status: pass
          result: "no matches"
        - cmd: "codex-peer-loop commit gate"
          status: pass
          result: "green, 9/10, no blockers after verified status and contract-test corrections"
        - cmd: "git push origin main"
          status: pass
  - id: T007
    type: pm
    assignee: PM
    status: done
    reasoning_hint: default
    objective: "Replace the Eneo-branded hidden publication, suggestion, review-metadata, and failure-status marker protocol with one review-agent protocol across every reader, writer, example, and affected test."
    constraints:
      - "The PM implements and verifies all production changes directly."
      - "Subagents may provide read-only research or review only."
      - "Change all coupled marker readers and writers atomically; do not retain dual readers, aliases, migrations, or compatibility markers."
      - "This new repository has no production Eneo comments or persisted deployment to migrate; incompatibility with old Eneo comment supersession is intentional."
      - "Do not change visible finding copy, ordering, scoring, suggestion eligibility, publication semantics, persistence schema, routes, environment names, policy behavior, or topology."
      - "Update existing marker behavior tests proportionally; add no marker matrix."
    expected_output:
      - "One review-agent hidden marker protocol for canonical publications, suggestions, review metadata, and failure statuses"
      - "No Eneo marker string outside archival goal receipts"
      - "Green focused publisher/suggestion/failure-status tests and bundle validation"
    verify:
      - "Focused review publisher, suggestion, failure-status, renderer, and memory tests."
      - "rg confirms no Eneo marker string remains outside archival goal receipts."
      - "pyright -p ."
      - "./scripts/check_bundle.sh"
      - "git diff --check"
    stop_if:
      - "A marker change requires a persisted-data migration, dual-read compatibility, or visible review behavior change."
      - "A focused verification fails twice for a non-mechanical reason."
    receipt:
      result: done
      summary: "Replaced all four hidden Eneo comment-marker protocols with their review-agent equivalents in the existing canonical owners, without aliases, migrations, or visible review changes."
      revision: "906b639"
      changed_files:
        - "Canonical publication, suggestion, review-metadata, and failure-status marker owners"
        - "Affected publisher, suggestion, failure-status, renderer, and memory tests"
        - "Published review example"
      commands:
        - cmd: "PYTHONPATH=bootstrap/plugins python3 -m unittest tests.test_review_publisher tests.test_review_suggestions tests.test_failure_status tests.test_memory_db"
          status: pass
          result: "120 tests"
        - cmd: "pyright -p ."
          status: pass
          result: "0 errors and 0 warnings"
        - cmd: "retired Eneo marker scan outside archival goal receipts"
          status: pass
          result: "no matches"
        - cmd: "./scripts/check_bundle.sh"
          status: pass
          result: "460 tests, two replay fixtures, strict Pyright, and YAML"
        - cmd: "codex-peer-loop commit gate"
          status: pass
          result: "green, 9/10, no findings after the exact diff resolved the initial evidence blocker"
        - cmd: "git push origin main"
          status: pass
  - id: T008
    type: pm
    assignee: PM
    status: done
    reasoning_hint: high
    objective: "Replace the Eneo-specific agent profile and repository-stack assumptions with one generic Sundsvalls kommun baseline policy, while preserving the review platform's fixed trust, snapshot, coverage, feedback, stale-head, and deterministic-publication rules."
    constraints:
      - "The PM implements and verifies all production changes directly."
      - "Subagents may provide read-only research or review only."
      - "Keep universal reviewer invariants in the baseline profile; move repository-stack, tenant, identity-provider, credential, retrieval, and MCP assumptions out of the organization-wide baseline instead of generalizing them into vague prose."
      - "Do not add repository-name conditionals, speculative policy-bundle loaders, compatibility text, deployment topology, schema changes, or new runtime abstractions."
      - "Do not weaken the untrusted-content boundary, exact base/head subject, bounded read coverage, human-governed feedback, stale-head protection, or deterministic publication path."
      - "Use the approved refactor plan as sequencing input; maintained Sundsvall frontend repositories remain examples for a later read-only policy-bundle task, not baseline truth."
    expected_output:
      - "Generic organization-wide SOUL and baseline AGENTS policy with no Eneo identity or repository-stack assumptions"
      - "Universal security and review invariants preserved in their existing owners"
      - "Focused documentation-contract proof and green bundle validation"
    verify:
      - "Focused documentation-contract tests for fixed reviewer invariants and generic baseline policy."
      - "rg confirms no Eneo identity remains in the active profile or baseline policy."
      - "pyright -p ."
      - "./scripts/check_bundle.sh"
      - "git diff --check"
    stop_if:
      - "The change requires choosing a concrete repository-specific policy-bundle format or runtime loader."
      - "A fixed security, snapshot, coverage, feedback, stale-head, or deterministic-publication invariant becomes ambiguous."
      - "A focused verification fails twice for a non-mechanical reason."
    receipt:
      result: done
      summary: "Replaced the Eneo project profile and stack-specific assumptions with a repository-neutral Sundsvall baseline across all three deployed profile owners, while preserving the fixed review and trust contract."
      revision: "99332e6"
      changed_files:
        - "Reviewer voice and organization-wide policy owners"
        - "Loaded pull-request procedure and Ponytail integration note"
        - "Focused documentation-contract test"
      commands:
        - cmd: "PYTHONPATH=bootstrap/plugins python3 -m unittest tests.test_docs_contract"
          status: pass
          result: "23 tests"
        - cmd: "retired profile, stack, and procedural-example scans"
          status: pass
          result: "no matches in the managed profile owners"
        - cmd: "pyright -p ."
          status: pass
          result: "0 errors and 0 warnings"
        - cmd: "./scripts/check_bundle.sh"
          status: pass
          result: "461 tests, two replay fixtures, strict Pyright, and YAML"
        - cmd: "codex-peer-loop commit gate"
          status: pass
          result: "green, 8/10, after adding the omitted live skill owner and neutralizing its repository-specific examples"
        - cmd: "git push origin main"
          status: pass
  - id: T009
    type: pm
    assignee: PM
    status: done
    reasoning_hint: default
    objective: "Remove remaining Eneo identity from private learning and coach tools, documentation headings, and replay fixtures while preserving the replay validator and human-governed learning behavior."
    constraints:
      - "The PM implements and verifies all production changes directly."
      - "Subagents may provide read-only research or review only."
      - "Replace historical Eneo replay data with a minimal neutral fixture set; do not add fixture volume or broaden replay semantics."
      - "Do not change learning governance, promotion thresholds, private/public boundaries, database schema, or runtime review behavior."
    expected_output:
      - "Generic private learning and coach surface"
      - "Neutral replay fixtures and no Eneo historical report"
      - "Green replay and bundle validation"
    verify:
      - "Focused coach, learning, export, replay, and structural replay tests."
      - "review-agent-memory validate-replay succeeds on the neutral fixture set."
      - "rg confirms no Eneo identity remains in active private learning code, documentation, replay fixtures, or reports."
      - "pyright -p ."
      - "./scripts/check_bundle.sh"
      - "git diff --check"
    stop_if:
      - "The cleanup requires a learning-governance, promotion-threshold, public/private-boundary, database-schema, or runtime-review behavior change."
      - "A neutral fixture cannot preserve the current structural replay validator contract without inventing new behavior."
      - "A focused verification fails twice for a non-mechanical reason."
    receipt:
      result: done
      summary: "Removed inherited Eneo product stories and historical replay evidence from the private learning surface, kept the structural validator strict, and made the bundle report an honest skip until human-confirmed replay evidence exists."
      revision: "b5753c9"
      changed_files:
        - "Private learning documentation and neutral coach/verification test narratives"
        - "Replay evidence inventory and historical Eneo report removal"
        - "Bundle handling for an intentionally empty historical replay inventory"
      commands:
        - cmd: "PYTHONPATH=bootstrap/plugins python3 -m unittest -v focused coach, learning, replay, export, and verification modules"
          status: pass
          result: "80 tests"
        - cmd: "direct replay validator against an empty temporary directory"
          status: pass
          result: "remains fail-closed; no synthetic evidence was added"
        - cmd: "private learning semantic identity and inherited-product-story scan"
          status: pass
          result: "no matches in the scoped active surface"
        - cmd: "pyright -p ."
          status: pass
          result: "0 errors and 0 warnings"
        - cmd: "./scripts/check_bundle.sh"
          status: pass
          result: "461 tests, explicit no-historical-fixture skip, strict Pyright, and YAML"
        - cmd: "codex-peer-loop commit gate"
          status: pass
          result: "green, 9/10, after removing disguised inherited stories and synthetic human-confirmed evidence"
        - cmd: "git push origin main"
          status: pass
  - id: T010
    type: scout
    assignee: Scout
    status: done
    reasoning_hint: high
    objective: "Map reusable frontend review conventions from Sundsvalls kommun's maintained starter and shared-component repositories for a later policy/profile slice."
    inputs:
      - "https://github.com/Sundsvallskommun/web-app-starter"
      - "https://github.com/Sundsvallskommun/web-shared-components"
    constraints:
      - "Read-only."
      - "Treat the repositories as verified examples, not canonical truth or a blueprint."
      - "Verify conventions against source before proposing policy."
      - "Do not broaden the current allowlist tranche."
      - "Do not name or copy from private inspiration sources in repository artifacts."
    expected_output:
      - "Verified reusable conventions"
      - "Repository-specific details to exclude"
      - "Candidate policy-bundle inputs"
    receipt:
      result: done
      note: "notes/T010-frontend-conventions.md"
      summary: "Verified conditional frontend review inputs from pinned maintained sources without adding framework assumptions to the generic baseline."
      evidence:
        - "web-app-starter eb2e817d5ac350df2a7204642c059a87fb64b77a on develop"
        - "web-shared-components e5214095a3ae66803f9df134d908124786d73710 on main"
        - "Existing design-system reuse, generated-contract ownership, strict typing, repository-owned gates, core styling ownership, curated exports, and Storybook accessibility"
      exclusions:
        - "Framework/router/backend/monorepo shape and current dependency pins"
        - "Authentication, localization, deployment, and release details"
        - "Storybook or shared-component package rules outside repositories that contain those owners"
      spawned_tasks: []
  - id: T011
    type: pm
    assignee: PM
    status: done
    reasoning_hint: default
    objective: "Remove the final retired product identity from visible examples and behavior-neutral test fixtures, and delete the unowned speculative code-graph proposal."
    constraints:
      - "The PM implements and verifies all changes directly."
      - "Subagents may provide read-only research or review only."
      - "Use neutral example repositories, actors, paths, and copy while preserving case-normalization, escaping, pagination, publication, and isolation semantics."
      - "Replace inherited tenant/document example narratives with generic authorization/resource-scope examples, but keep generic tenant-risk detection available for repositories that use it."
      - "Delete the optional graph proposal rather than generalizing an unowned future integration into product guidance."
      - "Do not change runtime code, schemas, review behavior, policy rules, thresholds, topology, or add compatibility text."
      - "Update existing exact-output contracts only where the neutral example changes; add no fixture matrix or branding-only test."
    allowed_files:
      - bootstrap/plugins/review_agent_tools/review_renderer.py
      - docs/OPERATIONS.md
      - examples/comments/example-review.md
      - examples/optional/code-review-graph.md
      - tests/test_changed_files_pager.py
      - tests/test_docs_contract.py
      - tests/test_failure_status.py
      - tests/test_feedback_bridge.py
      - tests/test_feedback_ingestion.py
      - tests/test_memory_db.py
      - tests/test_memory_stats.py
      - tests/test_pr_file_large.py
      - tests/test_review_publisher.py
      - tests/test_review_renderer.py
      - tests/test_review_runs.py
      - tests/test_review_suggestions.py
      - tests/test_run_tools.py
      - tests/test_tools_validation.py
    expected_output:
      - "One repository-neutral published review example"
      - "No tracked active product/source/docs/examples/tests reference to the retired identity"
      - "No speculative code-graph integration guidance without an owner"
      - "Green affected behavior tests and bundle validation"
    verify:
      - "Focused tests for every affected test module."
      - "Case-insensitive tracked-file scan confirms no retired identity outside archival goal receipts."
      - "pyright -p ."
      - "./scripts/check_bundle.sh"
      - "git diff --check"
    stop_if:
      - "A fixture replacement changes repository parsing, case-normalization, escaping, pagination, publication, or isolation behavior."
      - "Removing the optional graph proposal breaks a referenced product or operator contract."
      - "A focused verification fails twice for a non-mechanical reason."
    receipt:
      result: done
      summary: "Removed the final inherited identity from active examples and test data, replaced the remaining platform-owned tenancy assumption with generic data-isolation copy, preserved generic tenant-risk detection, and deleted the unowned speculative graph proposal."
      revision: "b797ebf"
      changed_files:
        - "Canonical fix-brief renderer and published review example"
        - "Neutral operator and behavior-test fixtures across repository, actor, authorization, publication, escaping, pagination, feedback, and lifecycle contracts"
        - "Deleted optional code-graph proposal"
      commands:
        - cmd: "PYTHONPATH=bootstrap/plugins python3 -m unittest -v all 14 affected behavior modules"
          status: pass
          result: "325 tests"
        - cmd: "renderer-copy red-green contract"
          status: pass
          result: "failed on the inherited tenancy sentence, then passed with generic data-isolation copy"
        - cmd: "case-insensitive active tracked-file retired-identity scan"
          status: pass
          result: "no matches outside archival goal receipts"
        - cmd: "pyright -p ."
          status: pass
          result: "0 errors and 0 warnings"
        - cmd: "./scripts/check_bundle.sh"
          status: pass
          result: "461 tests, strict Pyright, YAML, and explicit no-historical-replay skip"
        - cmd: "codex-peer-loop commit gate"
          status: pass
          result: "green, 8/10, after preserving one generic multitenancy risk test and correcting two inconsistent fixture phrases"
        - cmd: "git push origin main"
          status: pass
  - id: T999
    type: judge
    assignee: Judge
    status: active
    reasoning_hint: high
    objective: "Audit whether the generic identity/configuration slice satisfies the current tranche."
    inputs:
      - "All done task receipts"
      - "Last verification"
      - "Current dirty diff"
      - "Published main revision"
    constraints:
      - "Do not implement."
      - "Reject completion if required Worker work is queued or active."
      - "Reject added architecture that lacks a proven second adapter or caller need."
    expected_output:
      - "complete | not_complete"
      - "Missing evidence"
      - "Next task if not complete"
    receipt: null

checks:
  dirty_fingerprint: "T011-receipt-uncommitted"
  last_verification:
    result: pass
    task: T011
    commands:
      - "./scripts/check_bundle.sh"
      - "pyright -p ."
      - "325 focused affected behavior tests"
      - "case-insensitive active tracked-file retired-identity scan"
      - "codex-peer-loop green at 8/10"
