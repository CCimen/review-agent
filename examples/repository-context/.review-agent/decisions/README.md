# Repository decisions

Store typed, accepted design decisions in this directory and list each one in
`../decisions.toml`. An unlisted Markdown file is ignored by Review Agent.

Keep reusable platform context under `../context/`. Use a decision for a durable
repository invariant whose change needs explicit impact checks and evidence.
