# Replay evidence

Store only scrubbed, human-confirmed historical replay fixtures in this
directory. The generic baseline intentionally ships without replay evidence.

Do not add synthetic repositories, pull requests, or commit identifiers here:
the replay schema treats every source as human-confirmed historical evidence.
