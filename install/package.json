{
  "name": "review-agent-install-contract",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node validate.mjs --allow-example review-agent.example.yaml && (node validate.mjs review-agent.example.yaml; test $? -eq 1)"
  },
  "dependencies": {
    "ajv": "8.20.0",
    "yaml": "2.9.0"
  },
  "engines": {
    "node": ">=24.0"
  }
}
