# Review Agent

Project instructions live in `AGENTS.md`, shared with Codex and other agent
tools. Claude Code discovers only `CLAUDE.md`, so `AGENTS.md` is imported here.

@AGENTS.md

Work under `admin/` also loads `admin/CLAUDE.md`, the Astryx agent docs the
design-system CLI generates. Do not hand-edit its `ASTRYX` block.
